extends layout
block content
  .container
    h1=meta.title
    p
      em=meta.description
      br
      em Author: #{meta.author}
      br
      a(href='/raw/#{id}') (view raw)
  if err
    .error=err
  else
    .section
      .container
        pre: code= code